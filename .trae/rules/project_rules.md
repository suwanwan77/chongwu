及时删除项目中的多余测试文件与其他冗余文件，避免占用存储空间且影响文件查找
将所有 md 格式文件统一移动并合并到 doc 文件夹下，确保 md 文件集中管理
合并项目中的重要文档，删除重复或多余的文档，若有需要可新建文件夹进行分类存放
按功能模块区分并整理文件夹，分别创建 frontend（前端）、backend（后端）、db_backups（数据库备份）、doc（文档说明）文件夹，提升文件可读性与整理效率
在 doc 文件夹下按分类规范化创建子文件夹，包括 README（存放项目简介、安装 / 使用步骤、核心功能文档）、API（存放接口文档，需包含参数、返回值、示例内容）、DEVELOPMENT（存放开发环境配置、调试指南文档）、CHANGELO（存放版本变更记录文档）、外部 api（存放从外部对接的 api 说明文档）、初始设计（存放初始设计阶段的框架功能相关文件）
安装 Playwright 浏览器，用于项目相关测试工作
项目所有文档与注释均使用中文，确保团队成员理解无障碍
若项目部署在 Ubuntu 系统，使用 nohup 命令实现服务持久化运行，保证服务稳定
项目功能更新后需及时同步更新对应文档，非必要情况下不新增文档，避免文档冗余
制定代码规范文档，内容需包含：①代码命名需遵循统一规则（如变量用小驼峰、常量全大写）、②代码缩进与格式保持一致（如使用 4 个空格缩进）、③需添加必要的代码注释（如函数功能、复杂逻辑说明）、④避免冗余代码（如重复逻辑需封装为函数）、⑤代码提交前需进行语法检查与功能测试
制定数据库设计规范文档，内容需包含：①数据库表命名需体现业务含义且统一前缀（如用户表命名为 sys_user）、②字段类型选择需匹配数据需求（如时间用 datetime 类型、状态用 tinyint 类型）、③每张表需设置主键且建议使用自增 ID、④重要字段需添加索引（如频繁查询的关键字段）、⑤表与表之间的关联需通过外键明确，且外键需关联对应表的主键
针对 AI 测试产生的冗余文档，在项目根目录创建专门的 “AI_TEST_TEMP” 临时文件夹，所有 AI 生成的测试中间文档（如未验证的测试用例草稿、重复的测试报告）统一存放在此文件夹，每周定期清理无效文件，仅保留经测试团队确认有效的文档并迁移至 doc/DEVELOPMENT 或 test 文件夹
规范 AI 测试文档输出格式，要求 AI 生成测试相关文档时需包含 “测试场景”“测试用例编号”“预期结果”“实际结果” 核心字段，避免生成无明确用途的描述性内容，同时禁止 AI 绕过测试失败依赖生成文档，需在文档中明确标注依赖的测试用例编号及失败原因，待依赖问题解决后再更新文档
制定 AI 测试简化版本文档标准，对于 AI 生成的复杂测试文档（如包含多模块的综合测试报告），需提炼简化版本，简化版需包含 “核心测试结论”“关键问题列表”“修复建议” 3 项核心内容，字数控制在 500 字以内，便于快速查阅，简化版文档与完整版文档需关联存放（如简化版命名为 “测试报告_v1.0_简化版.md”，完整版命名为 “测试报告_v1.0_完整版.md”）
建立 AI 测试文档审核机制，AI 生成的测试文档需经测试负责人审核，确认无冗余内容、未绕过失败依赖且符合简化标准后，方可纳入正式文档管理，未通过审核的文档需返回调整或移入 “AI_TEST_TEMP” 临时文件夹待处理
在 doc/DEVELOPMENT 文件夹下新增《AI 测试文档管理规范.md》，明确 AI 测试文档的生成要求、存储路径、清理周期、审核流程及简化标准，确保团队成员遵循统一规则管理 AI 测试相关文档